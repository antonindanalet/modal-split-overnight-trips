The code will generate one PNG file by NUTS1 region here, with the name [latin_name_of_NUTS_region].png.
The latin name has been simplified (e.g. removing everything after a '/' in the name.
See also the function 'slugify' in main.py.